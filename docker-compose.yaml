services:
  via-api:
    build:
      context: .
      dockerfile: Dockerfile
    image: eizen-via-api
    container_name: via-api
    restart: unless-stopped
    volumes:
      - ./checkpoints:/usr/app/checkpoints
    deploy:
      resources:
        reservations:
          cpus: '0.50'
          memory: 1g
