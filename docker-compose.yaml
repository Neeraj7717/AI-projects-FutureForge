version: '3'

services:
  via-api:
    build:
      context: .
      dockerfile: Dockerfile
    image: eizen-via-api
    container_name: via-api
    restart: unless-stopped
    ports:  # Corrected from "port" to "ports"
      - "8751:8751"
    volumes:
      - ./checkpoints:/usr/app/checkpoints

