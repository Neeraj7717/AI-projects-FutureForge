version: '3'

services:
  via-api:
    build:
      context: .
      dockerfile: Dockerfile
    network_mode: host
    container_name: via-api
    restart: unless-stopped
    volumes:
      - ./checkpoints:/usr/app/checkpoints
